# Python
__pycache__/
*.py[cod]
.ruff_cache/
.pytest_cache/
.venv/
venv/

# Frame corpora. Hundreds of MB of PNGs that are fully regenerated by
# scripts/make_demo_data.py or scripts/fetch_game_frames.py — not source.
data/

# Training run outputs. checkpoints/pretrained/ is the one released model;
# everything else here is local scratch.
checkpoints/*
!checkpoints/pretrained/
checkpoints/pretrained/*
!checkpoints/pretrained/best.pt
!checkpoints/pretrained/history.json
!checkpoints/pretrained/eval_metrics.json
!checkpoints/pretrained/confusion_matrix.png

# Rebuilt from the templates by scripts/build_report.py.
visuals/report.html
